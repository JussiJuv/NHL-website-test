class Team {
    constructor(name, city, abb, id, conf, divi) {
        this.name = name;
        this.city = city;
        this.abb = abb;
        this.id = id;
        this.conf = conf;
        this.divi = divi;
    }
}